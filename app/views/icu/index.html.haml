- content_for :title, t("icu.index")

%h1.text-center= t("icu.index")

.row
  .col-md-6.col-md-offset-3
    %hr
    %ul
      %li= link_to t("icu.documents"), icu_documents_path
      %li= link_to t("icu.officers"), icu_officers_path
      %li= link_to t("icu.subscribers"), icu_subscribers_path
      %li= link_to t("icu.life_members"), icu_life_members_path
      %li= link_to_if can?(:sales_ledger, Item), t("item.sales_ledger"), sales_ledger_admin_items_path
    %hr
