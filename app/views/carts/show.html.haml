- content_for :title, t("shop.cart.cart")

.row
  .col-md-2
  .col-md-8
    %table{class: "table table-striped table-condensed"}
      %thead
        %tr
          %th.col-sm-5= t("shop.cart.item.item")
          %th.col-sm-4= t("shop.cart.item.member")
          %th.col-sm-2.text-center= t("shop.cart.item.cost")
          %td.col-sm-1
      %tbody
        - @cart.cart_items.each do |item|
          %tr
            %td= item.cartable.description
            %td= item.cartable.player.name(id: true)
            %td.text-center= euros(item.cartable.cost)
            %td.text-center= link_to "✘", item, data: { confirm: t("shop.cart.item.confirm_delete") }, method: :delete, class: "btn btn-danger btn-xs"
        %tr
          %th{colspan: 2}= t("shop.cart.total")
          %th.text-center= euros(@cart.total)
          %th
.row
  .col-md-2
  .col-md-3.text-center
    = link_to shop_path, class: "btn btn-info" do
      = t("shop.cart.continue")
  .col-md-2
  .col-md-3.text-center
    = link_to shop_path, class: "btn btn-info" do
      = t("shop.cart.checkout")
