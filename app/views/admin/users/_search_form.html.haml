= form_tag admin_users_path, method: :get, class: "form-inline", role: "form" do
  = render "utils/inline_text_field", param: :email, text: "Email"
  = render "utils/inline_select", param: :expiry, text: "Expiry", menu: expiry_search_menu(params[:expiry])
  = render "utils/inline_select", param: :status, text: "Status", menu: status_search_menu(params[:status])
  = render "utils/inline_select", param: :verify, text: "Verified", menu: verify_search_menu(params[:verify])
  = render "utils/inline_select", param: :role, text: "Role", menu: roles_search_menu(params[:role])
  = button_tag "Search", type: "submit", class: "btn btn-default pull-right"
